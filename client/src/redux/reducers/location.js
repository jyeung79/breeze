const locationReducer = (state=false, action) => {
    switch(action.type) {
        case '':
            return ;
        default:
            return state;
    }

};

export default locationReducer;